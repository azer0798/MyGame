<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>أدق حاسبة عمر احترافية 2026 | ميلادي وهجري وبرجك الفلكي</title>
    <meta name="description" content="احسب عمرك بدقة متناهية بالسنوات والأشهر والأيام. تعرف على برجك الفلكي، وعمرك بالهجري، وإحصائيات مدهشة عن حياتك.">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">
    <style>
        :root { --primary: #ff4d94; --dark: #2d3436; }
        body { background: #fdf2f7; font-family: 'Cairo', sans-serif; color: var(--dark); }
        .calc-card { background: #fff; border-radius: 25px; padding: 30px; box-shadow: 0 15px 35px rgba(255, 77, 148, 0.1); border: 2px solid #ffe6f0; }
        .result-box { display: none; margin-top: 25px; }
        .stat-card { background: #f8f9fa; border-radius: 15px; padding: 15px; margin-bottom: 10px; border-right: 5px solid var(--primary); }
        .zodiac-badge { background: #6c5ce7; color: white; padding: 5px 15px; border-radius: 50px; font-size: 14px; }
        .ad-space { background: #fff5f8; border: 1px dashed #ff4d94; text-align: center; padding: 15px; margin: 20px 0; border-radius: 10px; color: #ff4d94; font-size: 13px; }
        .btn-main { background: var(--primary); color: white; border-radius: 12px; padding: 15px; font-weight: bold; width: 100%; border: none; transition: 0.3s; }
        .btn-main:hover { background: #e04482; transform: translateY(-2px); }
    </style>
</head>
<body>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-7">
            <div class="ad-space">إعلان متجاوب هنا (AdSense)</div>
            
            <div class="calc-card shadow-lg">
                <div class="text-center mb-4">
                    <h1 class="fw-bold h3" style="color: var(--primary);">المحلل العمري الذكي 🚀</h1>
                    <p class="text-muted">أدخل تاريخ ميلادك للحصول على تحليل كامل</p>
                </div>

                <div class="mb-4">
                    <input type="date" id="birthdate" class="form-control form-control-lg text-center">
                </div>
                
                <button onclick="fullAnalysis()" class="btn-main">بدء التحليل الشامل</button>

                <div id="result" class="result-box">
                    <div class="text-center p-4 bg-light rounded-4 mb-4">
                        <span class="zodiac-badge mb-2" id="zodiacSign">جاري الحساب...</span>
                        <h2 class="fw-bold mt-2" id="mainAge" style="color: var(--primary);">0 سنة</h2>
                        <p class="text-muted" id="hijriDate"></p>
                    </div>

                    <div class="row g-2 text-center">
                        <div class="col-6"><div class="stat-card">أشهر<br><strong id="resMonths">0</strong></div></div>
                        <div class="col-6"><div class="stat-card">أسابيع<br><strong id="resWeeks">0</strong></div></div>
                        <div class="col-6"><div class="stat-card">أيام<br><strong id="resDays">0</strong></div></div>
                        <div class="col-6"><div class="stat-card">ساعات<br><strong id="resHours">0</strong></div></div>
                    </div>

                    <div class="mt-4 p-3 border rounded-4">
                        <h6 class="fw-bold text-center mb-3">هل تعلم؟ (تقديراً)</h6>
                        <ul class="list-unstyled small">
                            <li>🫀 نبض قلبك حوالي: <strong id="heartBeats">0</strong> مرة</li>
                            <li>😴 قضيت في النوم: <strong id="sleepTime">0</strong> يوم</li>
                            <li>📅 ولدت في يوم: <strong id="birthDayName">...</strong></li>
                        </ul>
                    </div>

                    <div class="alert alert-info mt-4 text-center">
                        🎁 عيد ميلادك القادم بعد: <strong id="nextBirthday">0</strong> يوم
                    </div>
                </div>
            </div>

            <div class="ad-space mt-4">إعلان يظهر بعد النتيجة (زيادة أرباح)</div>
        </div>
    </div>
</div>

<script>
function fullAnalysis() {
    const bInput = document.getElementById('birthdate').value;
    if (!bInput) return alert("من فضلك اختر تاريخ ميلادك!");

    const birth = new Date(bInput);
    const now = new Date();
    
    // 1. الحساب الأساسي
    let years = now.getFullYear() - birth.getFullYear();
    let months = now.getMonth() - birth.getMonth();
    let days = now.getDate() - birth.getDate();

    if (days < 0) { months--; days += new Date(now.getFullYear(), now.getMonth(), 0).getDate(); }
    if (months < 0) { years--; months += 12; }

    const totalDays = Math.floor((now - birth) / (1000 * 60 * 60 * 24));

    // 2. البرج الفلكي
    const zodiacs = [
        { name: "الجدي", day: 20, month: 1 }, { name: "الدلو", day: 19, month: 2 },
        { name: "الحوت", day: 20, month: 3 }, { name: "الحمل", day: 20, month: 4 },
        { name: "الثور", day: 21, month: 5 }, { name: "الجوزاء", day: 21, month: 6 },
        { name: "السرطان", day: 22, month: 7 }, { name: "الأسد", day: 23, month: 8 },
        { name: "العذراء", day: 23, month: 9 }, { name: "الميزان", day: 23, month: 10 },
        { name: "العقرب", day: 22, month: 11 }, { name: "القوس", day: 22, month: 12 }
    ];
    let month = birth.getMonth() + 1;
    let day = birth.getDate();
    let sign = zodiacs.find(z => (month === z.month && day <= z.day) || (month === z.month - 1 && day > zodiacs[month-2]?.day))?.name || "الجدي";

    // 3. اليوم الذي ولدت فيه
    const dayNames = ["الأحد", "الاثنين", "الثلاثاء", "الأربعاء", "الخميس", "الجمعة", "السبت"];
    
    // 4. عرض النتائج
    document.getElementById('result').style.display = 'block';
    document.getElementById('mainAge').innerText = `${years} سنة و ${months} شهر`;
    document.getElementById('zodiacSign').innerText = "برج " + sign;
    document.getElementById('resMonths').innerText = (years * 12) + months;
    document.getElementById('resWeeks').innerText = Math.floor(totalDays / 7).toLocaleString();
    document.getElementById('resDays').innerText = totalDays.toLocaleString();
    document.getElementById('resHours').innerText = (totalDays * 24).toLocaleString();
    document.getElementById('birthDayName').innerText = dayNames[birth.getDay()];
    
    // إحصائيات مدهشة
    document.getElementById('heartBeats').innerText = (totalDays * 24 * 60 * 70).toLocaleString(); // معدل 70 نبضة
    document.getElementById('sleepTime').innerText = Math.floor(totalDays / 3).toLocaleString(); // ثلث العمر نوم
    
    // تاريخ هجري تقريبي
    const hijriDate = new Intl.DateTimeFormat('ar-SA-u-ca-islamic', {day: 'numeric', month: 'long', year: 'numeric'}).format(birth);
    document.getElementById('hijriDate').innerText = "الموافق بالهجري: " + hijriDate;

    // العد التنازلي لعيد الميلاد
    let next = new Date(now.getFullYear(), birth.getMonth(), birth.getDate());
    if (now > next) next.setFullYear(now.getFullYear() + 1);
    document.getElementById('nextBirthday').innerText = Math.ceil((next - now) / (1000 * 60 * 60 * 24));
}
</script>
</body>
</html>
